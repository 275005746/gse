# GSE Evidence - 2026-07-16

## GSE-169 Budget-Aware Context Orchestrator

### Outcome

GSE now audits Codex context pressure, routes output and agent work by health, generates bounded execute-gated checkpoints, and feeds orange/red health into `/gse continue` rollover decisions.

### Evidence

- `node scripts/audit-context-orchestrator.mjs --root . --json`: passed 17/17.
- `node scripts/audit-context-health.mjs --target . --session-id 019f65e7-fa54-7f52-a45e-242bcef79d0b --json`: real rollout classified red with 100% exhaustion sentinel, 4 compactions, `new-task-required`, and `goal-payload-risk` for the 91,966-character goal-map.
- `node scripts/audit-continue-preflight.mjs --root . --json`: passed 42/42.
- `node scripts/audit-commands.mjs --root . --json`: passed 17/17.
- `node scripts/audit-capability-execution-matrix.mjs --root . --json`: passed 13/13 with 23 rows.
- `cmd /c npm run validate:lite`: passed 30/30; context orchestrator was included and passed 17/17.
- `cmd /c npm run check:encoding`: passed 825/825.
- Changed-script `node --check` commands: passed.
- `git diff --check`: passed with line-ending warnings only.

### Behaviors Proved

- Usage thresholds, compaction escalation, and the host exhaustion sentinel produce deterministic health and routes.
- Missing Codex rollout evidence degrades to `unavailable` without blocking portable GSE policy.
- Orange/red health disables scope expansion and makes `/gse continue` select `context-rollover`.
- Context packs are limited to 8 files and 8,000 estimated tokens, reject target escapes, and expose machine-readable selection/rejection evidence.
- Result capsules are capped at 800 estimated tokens and require status, summary, files inspected/changed, verification, evidence, residual risks, and next action.
- Tool-output pressure is reported as aggregates rather than raw output contents.
- Checkpoint generation is dry-run by default and writes only with `--execute`.

### Claim Boundary

This is `verified-component` evidence for portable context governance and explicit Codex rollout inspection. It does not compact a live host session, prevent active-goal reinjection, create a Codex task, or prove real subagent execution.

### Next Action

Commit, push, create PR, run CI/review, and merge to main.
